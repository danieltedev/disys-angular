import { MaskTelefoneDirective } from './mask-telefone.directive';

describe('MaskTelefoneDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskTelefoneDirective();
    expect(directive).toBeTruthy();
  });
});
